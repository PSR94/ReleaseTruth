{
  "name": "@releasetruth/api-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "exports": { ".": { "types": "./src/index.ts", "default": "./dist/index.js" } },
  "scripts": { "build": "tsc -p tsconfig.json", "lint": "tsc -p tsconfig.json --noEmit", "test": "vitest run" },
  "dependencies": {
    "@releasetruth/performance-adapter": "workspace:*",
    "@releasetruth/shared-types": "workspace:*"
  },
  "devDependencies": { "typescript": "^5.7.2", "vitest": "^2.1.8" }
}
